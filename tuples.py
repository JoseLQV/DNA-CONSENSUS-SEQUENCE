list_of_tuples = [(1, 'apple'), (2, 'banana'), (3, 'cherry')]
indices = [0, 1, 2]  # or any other list of indices you want to access


for j in range(1,len(list_of_tuples)):
    print(list_of_tuples[j])
